<Window x:Class="RustPlusDesk.Views.ServerInfoModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:RustPlusDesk.Localization"
        Title="{l:Loc server_info.window_title}"
        Width="450" Height="350"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" 
        WindowStyle="None"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border Background="{DynamicResource SurfaceAlt}" 
            BorderBrush="{DynamicResource CardBorder}" 
            BorderThickness="2" 
            CornerRadius="12">
        <DockPanel Margin="20">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,15">
                <DockPanel>
                    <TextBlock Text="{l:Loc server_info.title}"
                               Foreground="White"
                               FontSize="20"
                               FontWeight="Bold" 
                               VerticalAlignment="Center"/>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            Width="30" Height="30"
                            Style="{StaticResource GhostButton}"
                            Click="BtnClose_Click"
                            Cursor="Hand"
                            FontSize="18"
                            Foreground="{DynamicResource TextSubtle}"/>
                </DockPanel>
                <Separator Background="{DynamicResource CardBorder}" Margin="0,10,0,0"/>
            </StackPanel>

            <Button DockPanel.Dock="Bottom"
                    Content="{l:Loc common.close}"
                    Width="100"
                    Height="35"
                    HorizontalAlignment="Right"
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnClose_Click"
                    Margin="0,15,0,0"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="TxtDescription"
                           TextWrapping="Wrap"
                           Foreground="White"
                           FontSize="14"
                           LineHeight="20"
                           Text="{l:Loc server_info.no_description}"/>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
