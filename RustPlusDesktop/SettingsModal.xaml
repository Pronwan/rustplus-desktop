<Window x:Class="RustPlusDesk.Views.SettingsModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Application Settings"
        Width="500" Height="600"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" 
        WindowStyle="None"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border Background="{DynamicResource SurfaceAlt}" 
            BorderBrush="{DynamicResource CardBorder}" 
            BorderThickness="2" 
            CornerRadius="12">
        <DockPanel Margin="20">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <DockPanel>
                    <TextBlock Text="Settings" 
                               Foreground="White" 
                               FontSize="22" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"/>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            Width="30" Height="30"
                            Style="{StaticResource GhostButton}"
                            Click="BtnClose_Click"
                            Cursor="Hand"
                            FontSize="18"
                            Foreground="{DynamicResource TextSubtle}"/>
                </DockPanel>
                <Separator Background="{DynamicResource CardBorder}" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Footer -->
            <Button DockPanel.Dock="Bottom" 
                    Content="Close" 
                    Width="100" 
                    Height="35"
                    HorizontalAlignment="Right"
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnClose_Click"
                    Margin="0,15,0,0"/>

            <!-- Content -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="General" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <CheckBox x:Name="ChkAutoStart" 
                               Content="Launch with Windows (Minimized)" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkStartMinimized" 
                               Content="Start Minimized (Always)" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkAutoConnect" 
                               Content="Auto-Connect to last server on startup" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Automatically reconnects to the last server and loads the map."/>

                    <TextBlock Text="Behavior" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>

                    <CheckBox x:Name="ChkCloseToTray" 
                               Content="Minimize to Tray instead of closing" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkBackgroundTracking" 
                               Content="Enable Background Player Tracking" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Keeps tracking players via BattleMetrics even when minimized."/>

                    <CheckBox x:Name="ChkHideConsole" 
                               Content="Hide System Console (Declutter)" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Hides the bottom log/console panel to save map space."/>

                    <TextBlock Text="Map Settings" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    
                    <CheckBox x:Name="ChkAutoLoadShops" 
                               Content="Auto load shops on connection" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="Automatically starts polling shops and alerts when connected to a server."/>

                    <TextBlock Text="Discord Webhook" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    <TextBlock Text="Send alerts (Cargo, Heli, deaths, etc.) to a Discord channel via webhook. The same URL is used for every server you pair, the message shows which server fired it. Mute a single server with right-click on the server in the list."
                               Foreground="{DynamicResource TextSubtle}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtDiscordUrl"
                                 Grid.Column="0"
                                 Padding="6,4"
                                 FontSize="12"
                                 Background="{DynamicResource Surface}"
                                 Foreground="{DynamicResource TextPrimary}"
                                 BorderBrush="{DynamicResource CardBorder}"
                                 BorderThickness="1"
                                 ToolTip="Paste your Discord channel webhook URL (Server Settings → Integrations → Webhooks → New → Copy URL)"
                                 LostFocus="TxtDiscordUrl_LostFocus"/>
                        <Button x:Name="BtnDiscordTest"
                                Grid.Column="1"
                                Content="Test"
                                Width="64"
                                Margin="8,0,0,0"
                                Style="{StaticResource GhostButton}"
                                Click="BtnDiscordTest_Click"
                                ToolTip="Send a test message to the configured webhook"/>
                    </Grid>
                    <TextBlock x:Name="TxtDiscordStatus"
                               Foreground="{DynamicResource TextSubtle}"
                               FontSize="11"
                               Margin="0,0,0,4"/>

                    <TextBlock Text="Steam Account" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    <TextBlock Text="Manage your linked Steam account in the main window."
                               Foreground="{DynamicResource TextSubtle}"
                               FontSize="12"
                               FontStyle="Italic"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
