<ui:FluentWindow x:Class="RustPlusDesk.Views.Windows.AiHistoryWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="{DynamicResource AiHistoryTitle}"
                 Width="600" Height="560"
                 MinWidth="420" MinHeight="320"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{DynamicResource AiHistoryTitle}" ShowMaximize="False" />

        <TextBlock Grid.Row="1"
                   Margin="16,4,16,12"
                   TextWrapping="Wrap"
                   FontSize="11"
                   Foreground="{DynamicResource TextSubtle}"
                   Text="{DynamicResource AiHistoryIntro}" />

        <ScrollViewer Grid.Row="2" Margin="16,0,16,0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Entries" />
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="16,12,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TxtFooter"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Foreground="{DynamicResource TextSubtle}" />

            <ui:Button Grid.Column="1"
                       x:Name="BtnClear"
                       Content="{DynamicResource AiHistoryClear}"
                       Icon="{ui:SymbolIcon Symbol=Delete24}"
                       Appearance="Secondary"
                       Foreground="{DynamicResource DangerBrush}"
                       Height="32"
                       Click="BtnClear_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
