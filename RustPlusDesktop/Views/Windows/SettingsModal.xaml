<Window x:Class="RustPlusDesk.Views.SettingsModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:RustPlusDesk.Properties"
        Title="{x:Static properties:Resources.AppSettingsTitle}" 
        Width="500" Height="480"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" 
        WindowStyle="None"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border Background="{DynamicResource SurfaceAlt}" 
            BorderBrush="{DynamicResource CardBorder}" 
            BorderThickness="2" 
            CornerRadius="12">
        <DockPanel Margin="20">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <DockPanel>
                    <TextBlock Text="{x:Static properties:Resources.Settings}" 
                               Foreground="White" 
                               FontSize="22" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"/>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            Width="30" Height="30"
                            Style="{StaticResource GhostButton}"
                            Click="BtnClose_Click"
                            Cursor="Hand"
                            FontSize="18"
                            Foreground="{DynamicResource TextSubtle}"/>
                </DockPanel>
                <Separator Background="{DynamicResource CardBorder}" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Footer -->
            <Button DockPanel.Dock="Bottom" 
                    Content="{x:Static properties:Resources.Close}" 
                    Width="100" 
                    Height="35"
                    HorizontalAlignment="Right"
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnClose_Click"
                    Margin="0,15,0,0"/>

            <!-- Content -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource {x:Type ScrollViewer}}">
                <StackPanel>
                    <TextBlock Text="{x:Static properties:Resources.General}" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="{x:Static properties:Resources.Language}" VerticalAlignment="Center" Width="100" Foreground="{DynamicResource TextPrimary}"/>
                        <ComboBox x:Name="CmbLanguage" Width="150" 
                                  DisplayMemberPath="Name" SelectedValuePath="Code"
                                  SelectionChanged="CmbLanguage_SelectionChanged"/>
                    </StackPanel>
                    
                    <TextBlock x:Name="TxtRestartNote" 
                               Text="{x:Static properties:Resources.RestartRequired}" 
                               Foreground="#FFD166" FontSize="11" 
                               Margin="100,0,0,12" Visibility="Collapsed"/>
                    
                    <CheckBox x:Name="ChkAutoStart" 
                               Content="{x:Static properties:Resources.LaunchWithWindows}" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkStartMinimized" 
                               Content="{x:Static properties:Resources.StartMinimizedAlways}" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkAutoConnect" 
                               Content="{x:Static properties:Resources.AutoConnectLastServer}" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="{x:Static properties:Resources.AutoConnectToolTip}"/>

                    <TextBlock Text="{x:Static properties:Resources.Behavior}" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>

                    <CheckBox x:Name="ChkCloseToTray" 
                               Content="{x:Static properties:Resources.MinimizeToTray}" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"/>

                    <CheckBox x:Name="ChkBackgroundTracking" 
                               Content="{x:Static properties:Resources.BackgroundTracking}" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="{x:Static properties:Resources.BackgroundTrackingToolTip}"/>

                    <CheckBox x:Name="ChkHideConsole" 
                               Content="{x:Static properties:Resources.HideSystemConsole}" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="{x:Static properties:Resources.HideConsoleToolTip}"/>

                    <TextBlock Text="{x:Static properties:Resources.MapSettings}" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    
                    <CheckBox x:Name="ChkAutoLoadShops" 
                               Content="{x:Static properties:Resources.AutoLoadShops}" 
                               Margin="0,0,0,12" 
                               Style="{StaticResource DotCheckBox}"
                               Checked="OnSettingChanged" Unchecked="OnSettingChanged"
                               ToolTip="{x:Static properties:Resources.AutoLoadShopsToolTip}"/>

                    <!-- Chat Section -->
                    <TextBlock Text="{x:Static properties:Resources.Chat}" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Button Content="{x:Static properties:Resources.ModifyChatAlerts}" 
                                Style="{StaticResource PrimaryButton}"
                                Background="#3D5A80"
                                Height="36"
                                Padding="32,0"
                                Click="BtnModifyChatAlerts_Click"/>
                        <Button Content="{x:Static properties:Resources.ChatCommands}" 
                                Style="{StaticResource PrimaryButton}"
                                Background="#3D5A80"
                                Height="36"
                                Margin="12,0,0,0"
                                Padding="32,0"
                                Click="BtnChatCommands_Click"/>
                    </StackPanel>

                    <TextBlock Text="{x:Static properties:Resources.SteamAccount}" Foreground="{DynamicResource Accent}" FontWeight="Bold" Margin="0,10,0,10"/>
                    <TextBlock Text="{x:Static properties:Resources.ManageSteamAccountInfo}" 
                               Foreground="{DynamicResource TextSubtle}" 
                               FontSize="12" 
                               FontStyle="Italic"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
