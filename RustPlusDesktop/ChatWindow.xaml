<Window x:Class="RustPlusDesk.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Team-Chat" Height="420" Width="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Team Chat" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <ScrollViewer Grid.Row="1" Margin="0,0,0,8" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="TxtHistory" TextWrapping="Wrap"/>
        </ScrollViewer>

        <DockPanel Grid.Row="2">
            <TextBox x:Name="TxtChat"
                     DockPanel.Dock="Left"
                     MinWidth="360"
                     AcceptsReturn="True"
                     Height="60"
                     VerticalScrollBarVisibility="Auto"
                     KeyDown="TxtChat_KeyDown"/>
            <Button x:Name="BtnSend"
                    Content="Send"
                    Width="90"
                    Margin="8,0,0,0"
                    Click="BtnSend_Click"/>
        </DockPanel>
    </Grid>
</Window>
